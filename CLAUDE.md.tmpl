# Global Claude Code Instructions

> **Rule Precedence**: Project-specific rules (`.editorconfig`, linter configs, project `CLAUDE.md`) **always override** these global defaults.

## ⚠️ Golden Rules

1. **When in doubt, ASK!** — Never proceed without crucial information
2. **Be context-aware** — Minimize context usage; only include what's needed, remove what's not

## Communication Style

- **Tone**: Casual but professional
- **Emojis**: OK in status/headers, not in code/commits/docs
- **Humor**: Welcome, keep it light

## Versioning

**Always use latest versions** unless constrained by version pins, compatibility issues, or project requirements.

## Container Tooling

- **Local**: Use `podman` (docker is symlinked to it)
- **CI/Shared**: Standard Docker syntax for compatibility

## Git Workflow

### Core Rules
- **Always ASK before pushing** — never push without explicit user confirmation
- **Prefer `--amend`** on feature branches (never on `main`/`master`)
- **Check branch first**: `git branch --show-current`
- **Auto-detect repo type**: Check `git remote -v` for `github.com` vs `gitlab` (incl. self-hosted like `gitlab.example.com`) — don't ask

### Safe Amend
Verify HEAD before amending: `git log --oneline -1`

If target isn't HEAD, use fixup: `git commit --fixup=<sha>` then `git rebase -i --autosquash <sha>~1`

**Why?** Blind `--amend` can modify the wrong commit if something was committed in between (hook, CI, user).

### Pre-Commit
**Always lint/format before committing.** See `~/.claude/formatter-reference.md`

| Type | Check | Format |
|------|-------|--------|
| Python | `ruff check` | `ruff format` |
| Terraform | `terraform validate` | `terraform fmt` |
| YAML/JSON | `prettier --check` | `prettier --write` |

### Branch Naming
See `~/.claude/branch-naming.md` for naming conventions (change-based vs fun names).

### Grouping Changes
- **Split**: Different features, layers, or unrelated fixes
- **Single**: Tightly related or interdependent changes
- Use `git add -p` for hunk-level staging
{{ if .isWork }}
## GitLab CLI (glab)

| Action | Command |
|--------|---------|
| Create/View/List/Close MR | `glab mr create/view/list/close <id>` |
| Project info | `glab repo view` |
| Pipeline status | `glab ci status` |
{{ end }}
## File Formatting

Files end with newline, no trailing whitespace. See `~/.claude/formatter-reference.md`

## Commit Messages

Use conventional commits (`feat:`, `fix:`, `refactor:`, etc.)

### Co-Authored-By

| Repo Type | Format |
|-----------|--------|
| GitHub | `Co-Authored-By: Claude <noreply@anthropic.com>` (all commits) |
| GitLab | Initial: none. Subsequent: `Co-Authored-By: <Character> <email>` |

**GitLab characters**: Match vibe (bug fix → John Wick, delete → Thanos). Vary each commit. See `~/.claude/commit-flair.md`

**Initial commit** = first commit on a branch (even if branch has prior commits from base).
{{ if .isWork }}
## Work Environment

### AWS & Kubernetes
- SSO: `aws sso login --sso-session KN-Operations`
- Profiles: `dev`, `prod`
- Sync kubectl: `update_kubectl`

### Secrets (Bitwarden)
- `bw_get_secret "Item Name"` — auto-unlocks via `ensure_bw_session`
- Helpers: `~/.oh-my-zsh/custom/bitwarden.zsh`

### Jira
- `jira issue view <TICKET-ID>` (token via `set_jira_token`)

### Chezmoi (Dotfiles)
Home directory files must be added to chezmoi: `chezmoi add <file>` or `chezmoi re-add <file>`

**Templates** (`.tmpl`): Check with `ls ~/.local/share/chezmoi/ | grep <file>`. Edit templates directly — don't force-add over them.

**Always commit and push** chezmoi changes to `~/.local/share/chezmoi` after modifying dotfiles.
{{ end }}
