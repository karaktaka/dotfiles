key.txt.age
README.md
bootstrap.sh

# Work-only files - ignore on personal machines
{{ if not .isWork }}
.oh-my-zsh/custom/aws.zsh
.oh-my-zsh/custom/k8s.zsh
.claude/mcp-atlassian-wrapper.sh
.claude/.mcp.json
{{ end }}

# Plugin state managed by Claude Code - don't track versions
.claude/plugins/installed_plugins.json
.claude/plugins/known_marketplaces.json

# Shell-specific files - only deploy when the shell is installed
{{ if not (lookPath "zsh") }}
.oh-my-zsh/**
.zlogin
.zprofile
{{ end }}

{{ if not (lookPath "fish") }}
.config/fish/**
{{ end }}
