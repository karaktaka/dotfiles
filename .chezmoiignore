README.md
bootstrap.sh

# Personal-only files - ignore on work machines
{{ if .isWork }}
.claude/hooks/no-plans-folder.sh
{{ end }}

# Work-only files - ignore on personal machines
{{ if not .isWork }}
.local/bin/workspace-init
workspace/**
.config/kn-tools/**
.oh-my-zsh/custom/aws.zsh
.oh-my-zsh/custom/k8s.zsh
.oh-my-zsh/custom/confluence.zsh
.oh-my-zsh/custom/sso.zsh
.zshrc.d/**
.ssh/config.d/**
.gitconfig.d/**
.claude/bin/gitlab-post-review-note.sh
.claude/observability.md
.claude/kubernetes.md
.claude/work-environment.md
.claude/hooks/permissions-bash-work.sh
.claude/hooks/infra-safety.sh
.claude/hooks/permissions-webfetch.sh
Library/Application Support/mark.toml
{{ end }}

# Plugin state managed by Claude Code - don't track versions
.claude/plugins/installed_plugins.json
.claude/plugins/known_marketplaces.json

# Shell-specific files - only deploy when the shell is installed
{{ if not (lookPath "zsh") }}
.oh-my-zsh/**
.zlogin
.zprofile
{{ end }}

{{ if not (lookPath "fish") }}
.config/fish/**
{{ end }}

{{ if not (lookPath "fastfetch") }}
.config/fastfetch/**
{{ end }}

# macOS-only paths - ignore entirely on other OS
{{ if ne .chezmoi.os "darwin" }}
Library/**
{{ end }}

# k9s: work macOS only - on work macOS use Library path, everywhere else ignore all
{{ if and .isWork (eq .chezmoi.os "darwin") }}
.config/k9s/**
{{ else }}
.config/k9s/**
Library/Application Support/k9s/**
{{ end }}
