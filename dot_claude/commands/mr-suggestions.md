the branch you are on is a merge request. Look at the changes and summarize them.
 
You can get the diffs using the following command: `glab mr diff <git-branch-name> --raw`. Write it to `mr-diff.txt` in the cwd for easy reference while you are working through the task.
 
To do this, add a temporary file `mr-notes.md`. List all changed files and then go through them one at a time, looking at the diff and the relevant files. Make notes for yourself, describing the changes and their intentions. Once you have been through all files, review your notes and finish the task by providing a summary of the changes and their impact.
 
Keep the `mr-notes.md` file around after you provide your summary.
